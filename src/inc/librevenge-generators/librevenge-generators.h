/* Shim for the vendored build: the only generator we ship is
   RVNGSVGDrawingGenerator, whose header lives in librevenge/. */
#ifndef LIBREVENGE_GENERATORS_SHIM_H
#define LIBREVENGE_GENERATORS_SHIM_H
#include <librevenge/librevenge.h>
#endif
